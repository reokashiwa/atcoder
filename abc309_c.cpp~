#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int N, K;
  cin >> N >> K;
  if ((N < 1) || (N > 3 * 10e5))
    exit(1);
  if ((K < 0) || (K > 10e9))
    exit(1);
  vector<pai
}
