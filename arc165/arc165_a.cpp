#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int case() {
  int N;
  cin >> N;
  if ((N < 1) || (N > 10e9))
    exit(1);
  
}

int main() {
  
}
