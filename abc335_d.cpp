#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int N;
  cin >> N;
  if ((N < 3) || (N > 45))
    exit(1);
  if (N % 2 == 0)
    exit(1);
  vector<vector<int>> M(N, vector<int> (N));
  pair<int, int> p;
  p = make_pair(0, 0);
  rep (i, N * N) {
    
  }
}
