#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int H, W;
  cin >> H >> W;
  if ((H < 1) || (H > 1000))
    exit(1);
  if ((W < 1) || (W > 1000))
    exit(1);
  
  vector<vector<string>> S(H);
  rep (i, H) {
    string s;
    cin >> s;
    if (s.size() > W)
      exit(1);
    S[i] = s;
  }
}
