#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int N, M;
  cin >> N >> M;
  if ((N < 1) || (N > 1000))
    exit(1);
  if ((M < 1) || (M > 1000))
    exit(1);
  if (M > N)
    exit(1);
  
}
