#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int N, M;
  cin >> N >> M;
  vector<int> A(N);
  vector<int> B(M);
  rep (i, N) {
    int a;
    cin >> a;
    A[i] = a;
  }
  rep (i, M) {
    int b;
    cin >> b;
    B[i] = b;
  }
}
