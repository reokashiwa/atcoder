#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int H, W, N;
  if ((H < 1) || (H > 100))
    exit(1);
  if ((W < 1) || (W > 100))
    exit(1);
  vector<vector<char>> HW(H, vector<char> (W, '.'));
  if ((N < 1) || (N > 1000))
    exit(1);
  rep (i, N) {
    
  }
}
