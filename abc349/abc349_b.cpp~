#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  string S;
  cin >> S;
  int s = S.size();
  if ((s < 1) || (s > 100))
    exit(1);
  map<char, int> M;
  rep (i, s) {
    if (M.count(S[i])) {
      M[S[i]]++;
    } else {
      M[S[o]] = 0;
    }
  }
}
