#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int T;
  cin >> T;
  if ((T < 1) || (T > 2000))
    exit(1);
  rep (i, T) {
    
  }
}
