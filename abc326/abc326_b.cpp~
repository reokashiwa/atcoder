#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int N;
  cin >> N;
  if ((N < 100) || (N > 919))
    exit(1);

  for (int i = N; i < 920; i++) {
    int h = i / 100;
    int t = (i % 100) / 10;
    int o = (i % 100) % 10;
  
  }
}
