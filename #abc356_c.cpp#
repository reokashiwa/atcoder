#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int N, M, K;
  cin >> N >> M >> K;
  if ((N < 1) || (N > 15))
    exit(1);
  if ((M < 1) || (M > 100))
    exit(1);
  if ((K < 1) || (K > 15))
    exit(1);
  if (K > N)
    exit(1);

  rep (i, M) {
    int c;
    cin >> c;
    if ((c 
  }
  
}
