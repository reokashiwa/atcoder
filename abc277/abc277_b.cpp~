#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int N;
  cin >> N;
  if ((N < 1) || (N > 52))
    exit(1);
  vector<string> S(N);
  rep(i, N) {
    string s;
    cin >> s;
    rep(j, i) {
      if (S.at(j) != s)
	cout << "No" << endl;
    }
  }

}
