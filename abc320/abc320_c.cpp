#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int M;
  cin >> M;
  if ((M < 1) || (M > 100))
    exit(1);
  rep (i, 3) {
    string s;
    cin >> s;
    if (s.size() != M)
      exit(1);
    rep (j, M) {
      
    }
  }
}
