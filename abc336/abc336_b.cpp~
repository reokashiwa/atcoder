#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int N;
  cin >> N;
  if ((N < 1) || (N > 10e9))
    exit(1);

  int B = 0;
  rep (i, 30) {
    if (N > pow(2, i)) {
      B = i - 1;
      break;
    }
  }
}
