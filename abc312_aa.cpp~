#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  string S;
  cin >> S;
  if (S.size() != 3)
    exit(1);
  rep (i, 3) {
    if ((S[i] < 'A') || (S[i] > 'Z'))
      exit(1);
  }
  
}
