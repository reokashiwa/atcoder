#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int N, M;
  cin >> N >> M;
  if ((N < 2) || (N > 50))
    exit(1);
  if ((M < 0) || (M > N * (N - 1) /2))
    exit(1);
  vector<vector<int>> AB(M, vector<int> (M))
  
  rep (i, M) {
    int a, b;
    cin >> a >> b;
    if ((a < 1) || (a > N))
      exit(1);
    if ((b < 1) || (b > N))
      exit(1);
    if (a == b)
      exit(1);
    A[i] = a;
    B[i] = b;
  }
}
