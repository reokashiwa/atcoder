#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int N, Q;
  cin >> N >> Q;
  if ((N < 2) || (N > 10e6))
    exit(1);
  if ((N < 1) || (N > 2 * 10e5))
    exit(1);
  
  
}
