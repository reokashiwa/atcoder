#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int n;
  cin >> n;
  if ((n < 1) || (n > 2 * 10e5))
    exit(1);
  std::random_device rd;
  std::mt19937 mt(rd());
 
  rep (i, n) {
    cout << 'a' + (mt() % 26);
  }
}
