#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  string S_origin, S_sorted;
  cin >> S_origin;
  S_sorted = S_origin;
  sort(S_sorted.begin(), S_sorted.end());
}
