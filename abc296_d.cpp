#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  long long N, M;
  cin >> N >> M;
  if ((N < 1) || (N > 10e12))
    exit(1);
  if ((M < 1) || (M > 10e12))
    exit(1);
}
