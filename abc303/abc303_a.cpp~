#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int N;
  cin >> N;
  if ((N < 1) || (N > 100))
    exit(1);
  string S, T;
  cin >> S >> T;
  if (S.size() != N)
    exit(1);
  if (T.size() != N)
    exit(1);
  
}
