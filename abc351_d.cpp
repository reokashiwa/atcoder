#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int H, W;
  cin >> H >> W;
  if ((H < 1) || (H > 1000))
    exit(1);
  if ((W < 1) || (W > 1000))
    exit(1);
  vector<string> HW(H);
  rep (i, H) {
    string s;
    cin >> s;
    if (S.size() != W)
      exit(1);
    HW[i] = s;
  }
  vector<pair<int, int>> A;
  rep (i, H) {
    rep (j, W) {
      if (HW[i][j] != '#') {
	
      }
    }
  }
}
