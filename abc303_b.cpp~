#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int N, M;
  cin >> N >> M;
  if ((N < 2) || (N > 50))
    exit(1);
  if ((M < 1) || (N > 50))
    exit(1);
  vector<vector<int>> ANUM(N, vector<int>(N));
  
}
